{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel src/index.html --dist-dir ./dist -p 3000 ",
    "server": "parcel build src/index.html --no-source-maps --target default --dist-dir ./dist && node express.js",
    "build": "parcel build src/index.html --no-source-maps --target default --dist-dir ./dist",
    "test": "echo is ok"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
